server:
  port: 8585
spring:
  application:
    name: config-service
  cloud:
    config:
      label: standalone
      server:
        git:
          uri: https://github.com/devilmustcry/microservice-example-config.git