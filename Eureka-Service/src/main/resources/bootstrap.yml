server:
  port: 8888
spring:
  application:
    name: eureka-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/
    register-with-eureka: false
    fetch-registry: false
